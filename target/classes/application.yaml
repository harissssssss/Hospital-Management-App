server:
  port: 8989
spring:
  application:
    name: aarogyaniketan
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:mysql://localhost:5432/aarogyaniketan
    username: root
    pasword: password
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL95Dialect